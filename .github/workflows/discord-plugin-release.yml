name: Discord - Plugin Release

on:
  release:
    types: [published]

jobs:
  notify:
    runs-on: ubuntu-latest
    # Testing/prerelease builds (e.g. v3.10.x-testing) skip this - they're
    # for opted-in testers only, not a community-wide announcement. Only a
    # real stable release should post here.
    if: ${{ !github.event.release.prerelease }}
    steps:
      - name: Post to Discord
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          RELEASE_URL: ${{ github.event.release.html_url }}
        run: |
          curl -X POST "${{ secrets.DISCORD_WEBHOOK }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"embeds\": [{
                \"title\": \"🔌 Plugin Update Released\",
                \"description\": \"XIVVenueManagerSync **${RELEASE_TAG}** is now available.\",
                \"color\": 4751,
                \"fields\": [
                  {\"name\": \"🔗 Download\", \"value\": \"${RELEASE_URL}\"}
                ]
              }]
            }"
